import type { Command } from "./types.js";
import { notImplemented } from "./stub.js";

// Phase 1: interactive scaffold that writes a starter LOOP.md.
export const init: Command = () => notImplemented("init");
