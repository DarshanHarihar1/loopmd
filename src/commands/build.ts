import type { Command } from "./types.js";
import { notImplemented } from "./stub.js";

// Phase 3: parse → IR → compile → emit native artifacts.
export const build: Command = () => notImplemented("build");
