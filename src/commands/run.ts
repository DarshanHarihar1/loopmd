import type { Command } from "./types.js";
import { notImplemented } from "./stub.js";

// Phase 3: manually trigger a loop now (also called by the generated scheduler).
export const run: Command = () => notImplemented("run");
